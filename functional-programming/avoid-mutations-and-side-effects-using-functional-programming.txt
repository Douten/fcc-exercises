// The global variable
var fixedValue = 4;

function incrementer () {
  // Only change code below this line
  let increasedValue = fixedValue;
  increasedValue++;
  return increasedValue;
  // Only change code above this line
}